api:
  listen: ":1984"
  origin: "*"

rtsp:
  listen: ":8554"

webrtc:
  listen: ":8555"
  ice_servers:
    - urls: [ "stun:stun.l.google.com:19302" ]
  candidates:
    # ✅ ใส่ IP ของเครื่อง Server เพื่อให้ Browser ในวง LAN เชื่อมต่อได้โดยตรง
    - 192.168.123.123:8555
    - host:8555
    - stun:8555

# ✅ ใช้ FFmpeg ของระบบใน Container
ffmpeg:
  bin: "ffmpeg"

streams:
  # กล้องทดสอบ (ใช้สำหรับเช็คว่าระบบ Streaming ทำงานไหม)
  test_camera:
    - http://commondatastorage.googleapis.com/gtv-videos-bucket/sample/BigBuckBunny.mp4
